const CACHE_TTL = 5 * 60 * 1000; // cache for 5 mins
const DEFAULT_PERIOD_WEEK_COUNT = 16;
const MS_PER_WEEK = 7 * 24 * 60 * 60 * 1000;

module.exports = {
    CACHE_TTL,
    DEFAULT_PERIOD_WEEK_COUNT,
    MS_PER_WEEK,
};
