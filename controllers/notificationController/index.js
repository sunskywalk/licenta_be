// Routes must require `./notificationController/index`. Plain `./notificationController` resolves to a same-named `.js` file if it exists, not this folder.

module.exports = require('./controller');
